{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.py) bash scripts/check.sh ;; esac; } 2>/dev/null || true",
            "statusMessage": "Running scripts/check.sh"
          }
        ]
      }
    ]
  }
}
